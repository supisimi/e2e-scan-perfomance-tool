export function PerformanceChartPlaceholder() {
  return (
    <section className="panel">
      <h3>Chart Area (Prepared)</h3>
      <p className="muted">
        Recharts is installed. Add chart components here when performance trend visualization is implemented.
      </p>
    </section>
  );
}
